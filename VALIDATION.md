# 验证记录

日期：2026-09-11。环境：Windows，Node.js v24.9.0。

## 0.2.0 扩展覆盖验证

- 21 项 Node 测试全部通过，无跳过项，含真实资源包副本安装与精确恢复。
- 根据用户提供截图构造无 ARIA 的设置面板测试，验证标题、完整说明、拆分文字、动态打开、切换标签页及直接重开其他标签页。它是模拟 DOM，不是从客户端提取的 DOM，不等于真实视觉验收。
- 原生菜单翻译测试验证标签、快捷键、角色、回调及重复构建；真实菜单脚本通过 SHA-256 指纹检查后才允许追加代码。
- 验证旧补丁直接升级、仍只保留一份原始备份、升级后恢复与原资源包完全一致。
- WPF 实际窗口安装与恢复副本测试再次通过，含按钮状态、关闭保护、错误恢复及中文路径。
- 词库扩充至 654 条精确条目，针对用户反馈截图覆盖了反馈、浏览器提示、自定义、模型额度、外观设置及应用设置；未覆盖文案仍保留英文，不宣称 100% 汉化。
- 本机旧补丁已通过 `install` 原位更新，随后 `status` 确认新版已生效且原始恢复备份校验有效；真实设置页的视觉覆盖仍待重新打开确认。

## 已验证

- 15 项自动化测试全部通过，无跳过项；其中一项在真实 Antigravity 2.12.2 资源包的临时副本上执行安装与恢复。
- 副本补丁可由 Electron 官方 `@electron/asar` 4.3.0 读取，原 preload 保留，其他 ASAR 目录记录和原数据区保持不变。
- 恢复后副本与操作前完全一致；来源资源包未改变。
- DOM 测试验证精确词条、动态更新、大批量插入、深层保护区域、输入值不变、来源限制和重复初始化。
- 事务测试验证备份损坏、记录缺失、未知 preload、并发锁、客户端重新运行、文件变化和重命名失败时的行为。
- `node cli.js check` 检测本机版本与 preload 指纹匹配，当前未安装补丁。
- CLI 的 `install`、`status`、`restore` 也在本地资源包副本上实际执行成功，恢复后 SHA-256 与来源文件一致。
- `npm audit` 返回 0 项已知依赖漏洞；这只是查询时的依赖公告结果，不是完整安全审计。

开发期曾使用旧版 ASAR 测试依赖，其合成资源包生成后发生文件占用错误；升级独立测试读取器到 4.3.0 后全套通过。生产脚本不依赖 ASAR npm 包。

## 验证边界

### 图形启动器追加验证

- Windows PowerShell 5.1 / WPF 实际窗口成功打开，已渲染截图并检查中文显示、按钮和日志区域布局。
- `启动汉化工具.vbs` 实际启动成功，原生窗口响应正常；自动检测当前安装只读显示未汉化状态。
- 在 2.12.2 原资源包的临时副本上，通过真实窗口事件执行安装、状态刷新和恢复；恢复后副本与来源 SHA-256 相同。
- 验证操作时禁用按钮及路径输入、禁止关闭中断、路径变化清除旧状态、错误后重试、缺少 Node.js 提示。
- 测试路径包含中文、空格和 `&`，参数正确传递；来源客户端文件未被修改。
- 原有 Node 测试基线通过 14 项，真实资源包环境变量未设置时按约定跳过 1 项。汉化核心代码没有改动。

### Antigravity 客户端验收边界

- 没有给当前桌面客户端安装补丁，也没有进行真实桌面启动、交互和截图验收。DOM 模拟与资源包格式测试不证明完整桌面兼容性。
- 没有宣称全界面汉化，未覆盖未来版本、未知 DOM 布局和原生菜单。
- 自动测试中的合成客户端使用测试专用指纹模拟；生产代码允许列表只保留实际检查的 2.12.2 preload。
- GitHub CI 不持有 Google 安装包，因此真实副本用例会明确跳过；应以具体 Actions 运行结果为准。
